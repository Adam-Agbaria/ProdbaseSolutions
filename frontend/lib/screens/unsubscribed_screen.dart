import 'package:flutter/material.dart';
import '../widgets/unsubscribed_widget.dart';

class UnsubscribedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: UnsubscribedWidget(),
    );
  }
}
