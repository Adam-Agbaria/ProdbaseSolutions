class ProdConfig {
  static const String API_URL =
      'http://127.0.0.1:5000'; // replace with your production API endpoint
  static const String NODE_ENV = 'production';

  // If you have any frontend-specific configurations for production, add them here.
  // Examples could be analytics tokens, feature flags, etc.
  // Just ensure that no secret or sensitive data is stored here.
}
